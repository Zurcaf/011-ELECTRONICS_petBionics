; ── WiFi credentials ──────────────────────────────────────────────────────
; Este ficheiro é gitignored — nunca fazer commit.
; Copiar secrets.ini.template para secrets.ini e preencher os valores reais.

[secrets]
build_flags =
    -DWIFI_SSID=\"Cruz\"
    -DWIFI_PASSWORD=\"zurc2002\"
